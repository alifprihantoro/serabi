import links from '../../../configs/links/footer'
import { TITLE } from '../../../configs/global'
import { type TArgs } from './'

const args: TArgs = {
  name: TITLE,
  links,
}

export default args
